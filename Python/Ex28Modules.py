# Module is a simple python file that can contain classes, Functions and fields that can be used
# in other python files. Once the files are created, you must import them in other python files
# and consume it.

add_func = lambda fval, sval : fval + sval
sub_func = lambda fval, sval : fval - sval
mul_func = lambda fval, sval : fval * sval
div_func = lambda fval, sval : fval / sval

greet = lambda name: f'Hello {name}!!!'